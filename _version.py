"""Single source of truth for the gc-overlay version.

Read by main.py (for the --version flag) and bundled into frozen builds so
PRSH can display which gc-overlay it shipped.
"""

__version__ = "1.0.0"
