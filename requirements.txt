aiohttp>=3.9.0
# Dolphin process-memory transport (--transport dme). A ~340 KB compiled
# extension with no runtime dependencies; not the DME GUI application.
dolphin-memory-engine>=1.3.1
# Direct USB GC adapter (--usb) only; needs a native libusb at runtime.
pyusb>=1.2.1
